#include <stdio.h>
#include <stdlib.h>

int main()
{
    int data;

    printf("Digite o dia do seu nascimento: ");
    scanf ("%d", &data);

    printf("Dia do nascimento: %d", data);

    //char data [50];

    //printf ("digite o dia do seu nascimento: ");
    //fgets (data, sizeof(data), stdin);

    //printf("dia do nasciemnto: %s", data);
    return 0;
}
